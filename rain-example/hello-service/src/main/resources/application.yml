server:
  port: 9090
spring:
  application:
    name: @artifactId@

  datasource:
    url: jdbc:h2:mem:testdb;MODE=POSTGRESQL;
    username: sa
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true
      path: /h2-console

