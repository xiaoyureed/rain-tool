my_ip: '?'
spring:
  cloud:
    nacos:
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
      discovery:
        register-enabled: true
        server-addr: ${my_ip}:8848